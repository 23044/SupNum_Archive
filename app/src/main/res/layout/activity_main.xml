<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="#5a263d"
    android:padding="24dp">

    <!-- Conteneur centré verticalement -->
    <LinearLayout
        android:id="@+id/centerContainer"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_centerInParent="true"
        android:orientation="vertical"
        android:gravity="center_horizontal">

        <!-- Logo avec bordure -->
        <FrameLayout
            android:id="@+id/framelog"
            android:layout_width="180dp"
            android:layout_height="180dp"
            android:background="@drawable/logo_border_background"
            android:padding="4dp">

            <ImageView
                android:id="@+id/logo"
                android:layout_width="match_parent"
                android:layout_height="match_parent"
                android:scaleType="fitCenter"
                android:src="@drawable/logo_supnum_archive_radous" />
        </FrameLayout>

        <!-- Texte Bienvenue -->
        <TextView
            android:id="@+id/welcomeText"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginTop="40dp"
            android:text="Bienvenue sur SupNum Archives"
            android:textColor="#FFD088"
            android:textSize="22sp"
            android:textStyle="bold" />

        <!-- Texte Description -->
        <TextView
            android:id="@+id/descriptionText"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="16dp"
            android:gravity="center"
            android:paddingHorizontal="20dp"
            android:text="Accédez à toutes les ressources de SupNum en un clic !"
            android:textColor="#FFFFFF"
            android:textSize="16sp" />
    </LinearLayout>

    <!-- Boutons en bas -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_alignParentBottom="true"
        android:orientation="vertical"
        android:paddingBottom="24dp">

        <Button
            android:id="@+id/btn_login"
            android:layout_width="match_parent"
            android:layout_height="56dp"
            android:background="@drawable/btn_login_background"
            android:text="SE CONNECTER"
            android:textColor="#FFD088"
            android:textSize="16sp" />

        <Button
            android:id="@+id/btn_signup"
            android:layout_width="match_parent"
            android:layout_height="56dp"
            android:layout_marginTop="16dp"
            android:background="@drawable/btn_login_background"
            android:text="CRÉER UN COMPTE"
            android:textColor="#FFD088"
            android:textSize="16sp" />
    </LinearLayout>

</RelativeLayout>