<?xml version="1.0" encoding="utf-8"?>
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">

    <!-- Couleur de fond (transparente pour ne pas cacher le fond parent) -->
    <solid android:color="@android:color/transparent"/>

    <!-- Bordure colorée -->
    <stroke
        android:width="5dp"
        android:color="#57273d"/> <!-- Couleur violette de SupNum -->

    <!-- Coins arrondis -->
    <corners android:radius="190dp"/> <!-- Ajustez le rayon selon vos besoins -->
</shape>